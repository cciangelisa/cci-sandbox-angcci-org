version: 2
jobs:
  build:
    machine:
      image: circleci/classic:201711-01
  
    steps:
    - run:
        name: update apt
        command: apt-get update
workflows:
  version: 2
  my-build-workflow:
    jobs:
    - build
