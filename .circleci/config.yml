version: 2.1
jobs:
  build:
    docker:
    - image: circleci/node
    steps:
    - checkout
    - run:
        name: say hi
        command: echo "HI"
workflows:
  version: 2
  build-test-and-deploy:
    jobs:
    - build
