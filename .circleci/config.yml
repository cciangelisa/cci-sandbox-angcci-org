version: 2.1
jobs:
  build:
    docker:
    - image: circleci/node
    steps:
    - checkout
    - run:
        name: say hi
        command: sleep 120
workflows:
  version: 2
  build-test-and-deploy:
    jobs:
    - build
